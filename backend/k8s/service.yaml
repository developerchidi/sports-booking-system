apiVersion: v1
kind: Service
metadata:
  name: sports-booking-backend
  labels:
    app: sports-booking-backend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: sports-booking-backend 